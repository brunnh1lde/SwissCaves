
name: Build and test

on:
  pull_request:
    branches: [ master, main ]
  push:
    branches: [ master, main ]

jobs:
  build-and-test:
    uses: brunnh1lde/SwissCaves/.github/workflows/build-and-test.yml@master
    secrets: inherit
